'use client';
import Link from 'next/link';import {Menu,X,Search,MessageCircle} from 'lucide-react';import {useState} from 'react';import {site,wa} from '@/lib/config';
export default function Header(){const [open,setOpen]=useState(false);const links=[['Home','/'],['Buy','/buy'],['Rent','/rent'],['Projects','/projects'],['Sell / Lease','/sell'],['About','/about'],['Contact','/contact']];return <header className="sticky top-0 z-50 bg-paper/95 backdrop-blur border-b border-[#dfe4df]"><div className="container h-20 flex items-center justify-between"><Link href="/" className="flex items-center gap-3"><span className="w-10 h-10 rounded-xl bg-deep text-white grid place-items-center font-bold">DE</span><span className="font-bold tracking-tight text-lg">DeepEdge <span className="font-normal">Realty</span></span></Link><nav className="hidden lg:flex items-center gap-7 text-sm font-semibold">{links.map(([n,h])=><Link key={h} href={h} className="hover:text-accent">{n}</Link>)}</nav><div className="flex items-center gap-2"><Link href="/properties" className="hidden sm:flex btn btn-light gap-2"><Search size={16}/> Search</Link><a className="hidden md:flex btn btn-dark gap-2" href={wa('Hello DeepEdge Realty, I would like to enquire about a property.')} target="_blank"><MessageCircle size={16}/> WhatsApp</a><button className="lg:hidden p-2" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="lg:hidden container pb-5 grid gap-3">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} key={h} href={h} className="py-2 font-semibold">{n}</Link>)}<a href={wa('Hello DeepEdge Realty')} target="_blank" className="btn btn-dark">WhatsApp</a></nav>}</header>}
